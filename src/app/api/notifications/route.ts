import { NextResponse } from "next/server";import { authorized } from "@/shared/presentation/authorized";import { apiError } from "@/shared/presentation/api-error";import { prisma } from "@/shared/infrastructure/database/prisma";
export async function GET(){try{const c=await authorized("vacancies.read.own");const notifications=await prisma.notification.findMany({where:{tenantId:c.tenantId,userId:c.userId},orderBy:{createdAt:"desc"},take:50,select:{publicId:true,topic:true,title:true,body:true,actionUrl:true,readAt:true,createdAt:true}});return NextResponse.json({notifications,unread:notifications.filter(n=>!n.readAt).length})}catch(e){return apiError(e,"No fue posible consultar las notificaciones.")}}
