import { NextResponse } from "next/server";import { authorized } from "@/shared/presentation/authorized";import { apiError } from "@/shared/presentation/api-error";import { prisma } from "@/shared/infrastructure/database/prisma";import { ApplicationError } from "@/shared/domain/errors/application-error";
export async function POST(_:Request,{params}:{params:Promise<{publicId:string}>}){try{const c=await authorized("vacancies.read.own");const publicId=(await params).publicId;const result=await prisma.notification.updateMany({where:{publicId,tenantId:c.tenantId,userId:c.userId},data:{readAt:new Date()}});if(!result.count)throw new ApplicationError("La notificación no existe.","NOT_FOUND",404);return NextResponse.json({success:true})}catch(e){return apiError(e,"No fue posible actualizar la notificación.")}}
