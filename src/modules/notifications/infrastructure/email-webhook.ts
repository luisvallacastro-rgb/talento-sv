import { ApplicationError } from "@/shared/domain/errors/application-error";
export type EmailDelivery={idempotencyKey:string;to:string;template:"password-reset"|"invitation";variables:Record<string,string>};
export async function deliverEmail(delivery:EmailDelivery):Promise<void>{const url=process.env.EMAIL_WEBHOOK_URL;const token=process.env.EMAIL_WEBHOOK_TOKEN;if(!url||!token)throw new ApplicationError("El proveedor de correo no está configurado.","EMAIL_PROVIDER_NOT_CONFIGURED",503);const response=await fetch(url,{method:"POST",headers:{"content-type":"application/json","authorization":`Bearer ${token}`,"idempotency-key":delivery.idempotencyKey},body:JSON.stringify(delivery),signal:AbortSignal.timeout(10_000)});if(!response.ok)throw new ApplicationError(`El proveedor de correo respondió ${response.status}.`,"EMAIL_DELIVERY_FAILED",502)}
