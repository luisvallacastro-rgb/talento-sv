import assert from "node:assert/strict";import test from "node:test";import { calculateScore,validateDefinition,type ScoreDefinition } from "./scoring-engine";
const matrix:ScoreDefinition={variables:[{code:"experience",name:"Experiencia",weight:60,minimum:0,maximum:10,required:true},{code:"interview",name:"Entrevista",weight:40,minimum:0,maximum:100,required:true}],rules:[{code:"minimum_experience",description:"Experiencia mínima",condition:{variable:"experience",operator:"LTE",value:1},effect:{type:"EXCLUDE"}},{code:"strong_interview",description:"Entrevista sobresaliente",condition:{variable:"interview",operator:"GTE",value:90},effect:{type:"BONUS",amount:3}}]};
test("calcula componentes ponderados y reglas de forma reproducible",()=>{const a=calculateScore(matrix,{experience:5,interview:90});const b=calculateScore(matrix,{experience:5,interview:90});assert.deepEqual(a,b);assert.equal(a.total,69);assert.equal(a.appliedRules[0]?.code,"strong_interview")});
test("marca criterios excluyentes sin tomar una decisión de contratación",()=>assert.equal(calculateScore(matrix,{experience:1,interview:100}).excluded,true));
test("rechaza matrices cuyos pesos no suman cien",()=>assert.throws(()=>validateDefinition({...matrix,variables:[{...matrix.variables[0],weight:50},matrix.variables[1]]}),/100%/));
