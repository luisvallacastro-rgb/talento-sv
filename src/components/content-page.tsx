import { SiteHeader } from "./site-header"; import { SiteFooter } from "./site-footer";
export function ContentPage({ eyebrow, title, intro, children, className = "" }: { eyebrow: string; title: string; intro: string; children: React.ReactNode; className?: string }) { return <><SiteHeader/><main className={`content-main ${className}`}><section className="page-hero"><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{intro}</p></section>{children}</main><SiteFooter/></>; }
