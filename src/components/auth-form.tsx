"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export function LoginForm(){const router=useRouter();const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const data=Object.fromEntries(new FormData(e.currentTarget));const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const result=await response.json();setBusy(false);if(response.ok)router.push("/portal");else setMessage(result.error?.message??"No fue posible iniciar sesión.")}return <form className="public-form" onSubmit={submit}><div className="demo-access-note"><strong>Acceso de demostración</strong><span>Los datos ya están precargados. Pulse “Iniciar sesión”.</span></div><label>Empresa<input name="tenantSlug" required defaultValue="grupo-horizonte"/></label><label>Correo electrónico<input name="email" type="email" required defaultValue="cliente@grupohorizonte.demo"/></label><label>Contraseña<input name="password" type="password" minLength={8} required defaultValue="ClienteDemo2026!"/></label><button className="button" disabled={busy}>{busy?"Ingresando…":"Iniciar sesión"}</button><a className="text-link" href="/recuperar-contrasena">¿Olvidó su contraseña?</a>{message&&<p className="form-status error" role="alert">{message}</p>}</form>}

export function ForgotPasswordForm(){const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get("email");const response=await fetch("/api/auth/password/forgot",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const result=await response.json();setMessage(result.message??result.error?.message)}return <form className="public-form" onSubmit={submit}><label>Correo electrónico<input name="email" type="email" required/></label><button className="button">Enviar instrucciones</button>{message&&<p className="form-status success" role="status">{message}</p>}</form>}
