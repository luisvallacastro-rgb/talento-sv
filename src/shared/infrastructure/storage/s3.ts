import { GetObjectCommand, PutObjectCommand, S3Client } from "@aws-sdk/client-s3";import { getSignedUrl } from "@aws-sdk/s3-request-presigner";import { ApplicationError } from "@/shared/domain/errors/application-error";
function config(){const bucket=process.env.S3_BUCKET,region=process.env.S3_REGION,accessKeyId=process.env.S3_ACCESS_KEY_ID,secretAccessKey=process.env.S3_SECRET_ACCESS_KEY;if(!bucket||!region||!accessKeyId||!secretAccessKey)throw new ApplicationError("El almacenamiento privado no está configurado.","STORAGE_NOT_CONFIGURED",503);return{bucket,client:new S3Client({region,endpoint:process.env.S3_ENDPOINT||undefined,forcePathStyle:Boolean(process.env.S3_ENDPOINT),credentials:{accessKeyId,secretAccessKey}})}}
export async function signedUploadUrl(key:string,mimeType:string){const {bucket,client}=config();return getSignedUrl(client,new PutObjectCommand({Bucket:bucket,Key:key,ContentType:mimeType,ServerSideEncryption:"AES256"}),{expiresIn:300})}
export async function signedDownloadUrl(key:string,filename:string){const {bucket,client}=config();return getSignedUrl(client,new GetObjectCommand({Bucket:bucket,Key:key,ResponseContentDisposition:`attachment; filename="${filename.replace(/["\\]/g,"")}"`}),{expiresIn:120})}
